import React from 'react';

import LoginPage from './components/LoginPage';

/**
 * Root application component.
 *
 * Renders the LoginPage directly — no routing required for this stage.
 */
function App(): React.JSX.Element {
  return <LoginPage />;
}

export default App;
